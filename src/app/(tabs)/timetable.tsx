import { SafeArea, Section } from '@/components/layout'
import { Text } from '@/uikit'

export default function TimetableScreen() {
  return (
    <SafeArea>
      <Section>
        <Text type="title3">시간표</Text>
        <Text type="body2" color="#666">시간표 화면 준비 중</Text>
      </Section>
    </SafeArea>
  )
}
